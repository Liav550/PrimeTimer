import { Box } from "@mui/material";

export const InfoScreen = () => {
  return (
    <Box
      sx={{
        fontSize: "2rem",
        textAlign: "center",
        fontWeight: 800,
        backgroundColor: "rgba(0,0,0,0.2)",
        padding: "1rem",
      }}
    >
      Info Screen Here
    </Box>
  );
};
